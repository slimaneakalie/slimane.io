import { makeStyles } from "@mui/material/styles";

export const useRecentArticlesStyles = makeStyles(() => ({
  root: {
    display: "flex",
    alignItems: "center",
    flexDirection: "column",
    padding: "20px 0px",
  },
}));
